import 'package:flutter/material.dart';

class ListOfRequests extends StatelessWidget {
  const ListOfRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
